{
  "name": "cportfe",
  "version": "1.0.0",
  "description": "Serves the UI frontend of the Customer Portal",
  "scripts": {
    "build": "npm run compile",
    "compile": "node ./node_modules/typescript/bin/tsc --project tsconfig.json",
    "test": "./node_modules/.bin/mocha --require ./node_modules/ts-node/register/transpile-only -c --recursive */*.test.ts",
    "reset": "rm -rf ./node_modules && rm -rf ./dist && npm install"
  },
  "main": "index.ts",
  "keywords": [
    "custom",
    "typescript"
  ],
  "author": "Namrata Gupta",
  "license": "UNLICENCED",
  "devDependencies": {
    "@types/chai": "^4.2.12",
    "@types/mocha": "^8.0.0",
    "@types/node": "^12.12.38",
    "chai": "~4.2.0",
    "mocha": "^8.0.1",
    "ts-node": "^8.10.2",
    "typescript": "^4.2.2"
  },
  "dependencies": {
    "express": "^4.17.1",
    "https": "^1.0.0"
  }
}
